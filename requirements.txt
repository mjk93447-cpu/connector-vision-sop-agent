# PyTorch CPU (Actions Windows 호환 버전)
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Core Vision/ML
ultralytics>=8.4.0  # YOLO26 지원 최소 버전 (출시: 2026-01-14)
opencv-python-headless==4.10.0.84
pyyaml==6.0.2

# GUI (Phase 1+)
PyQt6>=6.7.0

# Build
pyinstaller==6.10.0

# Local/runtime
jsonschema>=4.0
mss>=9.0              # Screenshot capture for LLM panel 📸
Pillow==10.3.0
numpy==1.26.4
pyautogui==0.9.54
requests>=2.32.0
huggingface_hub>=0.21.0
datasets>=2.19.0

# OCR (optional — loaded lazily at runtime)
# Priority: winsdk (WinRT, Win10 built-in) → easyocr → paddleocr
winsdk>=1.0.0b9       # Windows.Media.Ocr WinRT wrapper (Windows 10 1803+ only)
easyocr>=1.7.0        # Fallback OCR (PyTorch-based, works without WinRT)
# PaddleOCR optional legacy fallback (broken in 3.x; 2.x requires separate install):
# paddlepaddle          # CPU paddle runtime (~200 MB); uncomment to install
# paddleocr>=2.8.0      # PP-OCRv4 Lite engine; uncomment to install
