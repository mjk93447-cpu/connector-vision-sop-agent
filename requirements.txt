ultralytics==8.4.0
transformers[torch]==4.56.2
surya-ocr==0.17.1

# PyTorch (install via -f torch_stable.html in CI when needed)
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

onnxruntime==1.19.0
accelerate==1.1.0

# bitsandbytes is not reliably available on Windows CPU.
bitsandbytes==0.44.0; platform_system != "Windows"

pyinstaller==6.10.0
pillow==10.4.0
opencv-python-headless==4.11.0.86
pytesseract==0.3.13
pyyaml==6.0.2
pyautogui==0.9.54
