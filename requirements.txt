# PyTorch CPU (Actions Windows 호환 버전)
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Core Vision/ML
ultralytics
opencv-python-headless==4.10.0.84
pytesseract
pyyaml==6.0.2

# PyInstaller (명시적 추가 - build.bat 필수)
pyinstaller==6.10.0

# 기타 (로컬에서 사용)
pillow
numpy
