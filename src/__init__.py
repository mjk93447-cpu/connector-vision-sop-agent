"""
Connector Vision SOP Agent source package.

This file makes the `src` directory importable as a Python package so that
tests and tools can use imports like `from src.sop_executor import SopExecutor`.
"""
